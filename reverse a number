#include <stdio.h>

int main(void) {
    
int num,reversenum = 0,remainder;
scanf("%d",&num);
while(num != 0)
{
    remainder = num % 10;
    reversenum = reversenum * 10 + remainder;
    num /= 10;
}
printf("%d",reversenum);
return 0;
}
